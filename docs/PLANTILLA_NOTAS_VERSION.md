# Plantilla de notas de versión - orioncop

Usar esta plantilla para cada release y guardarla en `docs/RELEASE_NOTES/<TAG>.md`.

## Actualización de la línea base operativa
Con esta versión se deja alineada la base operativa de OrionCop con la versión `<VERSION_PRINCIPAL>` y se corrigen ajustes necesarios para mantener la operación estable y consistente.

## Cambios relevantes
- `<CAMBIO_RELEVANTE_1>`
- `<CAMBIO_RELEVANTE_2>`
- `<CAMBIO_RELEVANTE_3>`
- Se deja la línea base operativa del ejecutable principal actualizada y sincronizada con la entrega de producción.

## Impacto en la operación
`<DESCRIBIR_IMPACTO_OPERATIVO_EN_NEGOCIO_Y_COMPONENTES_AFECTADOS>`

## Nota
Esta entrega prioriza la estabilidad operativa y la continuidad del proceso, manteniendo la base del sistema alineada con los ajustes necesarios del negocio y la tecnología.
